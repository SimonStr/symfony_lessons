{% extends 'base.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row col-xs-6 col-xs-offset-3">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-primary pull-right" type="submit" formnovalidate>Login</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}